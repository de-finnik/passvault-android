<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/background</item>
        <item name="android:textColorHint">#a8a8a8</item>
    </style>

    <style name="PassEditText">
        <item name="android:background">@color/foreground</item>
        <item name="android:textColor">@color/background</item>
        <item name="android:padding">10dp</item>
    </style>

    <style name="test">
        <item name="colorControlActivated">#FFFFFF</item>

        <!-- inactive thumb color -->
        <item name="android:thumbTint">#FFFFFF</item>

        <!-- inactive track color (30% transparency) -->
        <item name="android:colorForeground">#595959</item>
    </style>
</resources>
